---
phase: 01-infrastructure-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - package.json
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-09
  - D-10
  - D-17
user_setup:
  - service: supabase
    why: "Hosted PostgreSQL database and file storage"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string (Direct connection, port 5432, NOT transaction pooler port 6543)"
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role key (keep secret, never expose with EXPO_PUBLIC_ prefix)"
    dashboard_config:
      - task: "Create a Supabase project"
        location: "https://supabase.com/dashboard -> New Project"
      - task: "Create a public storage bucket named 'images'"
        location: "Supabase Dashboard -> Storage -> New Bucket -> Name: images, Public: ON"

must_haves:
  truths:
    - "All FK columns in shared/schema.ts have .references() with appropriate onDelete behavior"
    - "A sessions table exists in the schema with token, userId, createdAt, expiresAt columns"
    - "package.json has db:generate and db:migrate scripts using drizzle-kit"
    - "New dependencies @supabase/supabase-js and express-rate-limit are installed"
    - "Password minimum is 8 characters, not 6"
  artifacts:
    - path: "shared/schema.ts"
      provides: "All table definitions with FK constraints + sessions table"
      contains: "export const sessions = pgTable"
    - path: "package.json"
      provides: "Migration scripts and new dependencies"
      contains: "db:generate"
  key_links:
    - from: "shared/schema.ts"
      to: "drizzle.config.ts"
      via: "schema field in drizzle config"
      pattern: "schema.*shared/schema"
---

<objective>
Harden the Drizzle schema with foreign key constraints, add a persistent sessions table, install new dependencies, and switch migration tooling from db:push to db:generate + db:migrate.

Purpose: Foundation for all subsequent plans. FK constraints ensure data integrity on the fresh Supabase database (D-05). The sessions table enables persistent auth (D-09/D-10). New dependencies are used by Plans 03-04.
Output: Updated shared/schema.ts with FK constraints and sessions table. Updated package.json with new deps and migration scripts.
</objective>

<execution_context>
@.planning/phases/01-infrastructure-migration/01-CONTEXT.md
@.planning/phases/01-infrastructure-migration/01-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@shared/schema.ts
@package.json
@drizzle.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FK constraints, sessions table, and password hardening to schema</name>
  <files>shared/schema.ts</files>
  <read_first>shared/schema.ts</read_first>
  <action>
Modify shared/schema.ts. Three changes: add sessions table (D-09/D-10), add .references() to all FK columns (D-05), raise password min to 8 (D-17).

1. Add sessions table after the users table definition:

```typescript
export const sessions = pgTable("sessions", {
  token: varchar("token").primaryKey(),
  userId: varchar("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  expiresAt: timestamp("expires_at").notNull(),
});
```

2. Add .references() to every FK column. Complete list:

communities.createdBy -> .references(() => users.id, { onDelete: "set null" })
userCommunities.userId -> .references(() => users.id, { onDelete: "cascade" })
userCommunities.communityId -> .references(() => communities.id, { onDelete: "cascade" })
rucks.userId -> .references(() => users.id, { onDelete: "cascade" })
rucks.communityId -> .references(() => communities.id, { onDelete: "set null" })
rucks.challengeId -> .references(() => challenges.id, { onDelete: "set null" })
challenges.communityId -> .references(() => communities.id, { onDelete: "cascade" })
challenges.createdBy -> .references(() => users.id, { onDelete: "set null" })
challengeParticipants.challengeId -> .references(() => challenges.id, { onDelete: "cascade" })
challengeParticipants.userId -> .references(() => users.id, { onDelete: "cascade" })
communityPosts.communityId -> .references(() => communities.id, { onDelete: "cascade" })
communityPosts.userId -> .references(() => users.id, { onDelete: "cascade" })
ruckLikes.ruckId -> .references(() => rucks.id, { onDelete: "cascade" })
ruckLikes.userId -> .references(() => users.id, { onDelete: "cascade" })
ruckComments.ruckId -> .references(() => rucks.id, { onDelete: "cascade" })
ruckComments.userId -> .references(() => users.id, { onDelete: "cascade" })
notifications.userId -> .references(() => users.id, { onDelete: "cascade" })
notifications.fromUserId -> .references(() => users.id, { onDelete: "set null" })
friendships.requesterId -> .references(() => users.id, { onDelete: "cascade" })
friendships.addresseeId -> .references(() => users.id, { onDelete: "cascade" })

Drizzle handles forward references (rucks.challengeId -> challenges.id even though challenges is defined later) because the arrow function is evaluated lazily.

3. Change registerSchema password validation from min(6) to min(8):
```typescript
password: z.string().min(8),
```

4. Add type export at the bottom:
```typescript
export type Session = typeof sessions.$inferSelect;
```
  </action>
  <verify>
    <automated>cd "C:/Users/bpeeb/Ruck On App/RuckON-Mobile" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - shared/schema.ts contains `export const sessions = pgTable("sessions"`
    - shared/schema.ts contains `.references(() => users.id, { onDelete: "cascade" })` at least 10 times
    - shared/schema.ts contains `.references(() => communities.id` at least 3 times
    - shared/schema.ts contains `.references(() => rucks.id` at least 2 times
    - shared/schema.ts contains `.references(() => challenges.id` at least 2 times
    - shared/schema.ts contains `password: z.string().min(8)` NOT `min(6)`
    - shared/schema.ts contains `export type Session`
    - shared/schema.ts contains `expiresAt: timestamp("expires_at").notNull()`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All FK columns have .references() with correct onDelete. Sessions table with token PK, userId FK, createdAt, expiresAt. Password min raised to 8. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and add migration scripts</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
1. Run npm install for new dependencies:
```bash
npm install @supabase/supabase-js express-rate-limit
```

2. Add three new scripts to package.json "scripts" section (keep existing db:push):
- "db:generate": "drizzle-kit generate"
- "db:migrate": "drizzle-kit migrate"
- "db:seed": "tsx scripts/seed.ts"

Do NOT modify drizzle.config.ts — it already has out: './migrations' and reads DATABASE_URL.
  </action>
  <verify>
    <automated>cd "C:/Users/bpeeb/Ruck On App/RuckON-Mobile" && node -e "const p=require('./package.json'); const ok = p.dependencies['@supabase/supabase-js'] && p.dependencies['express-rate-limit'] && p.scripts['db:generate'] && p.scripts['db:migrate'] && p.scripts['db:seed']; console.log(ok ? 'PASS' : 'FAIL'); process.exit(ok ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies includes "@supabase/supabase-js"
    - package.json dependencies includes "express-rate-limit"
    - package.json scripts includes "db:generate": "drizzle-kit generate"
    - package.json scripts includes "db:migrate": "drizzle-kit migrate"
    - package.json scripts includes "db:seed": "tsx scripts/seed.ts"
    - node_modules/@supabase/supabase-js exists
    - node_modules/express-rate-limit exists
  </acceptance_criteria>
  <done>@supabase/supabase-js and express-rate-limit installed. Migration scripts db:generate, db:migrate, db:seed added to package.json.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit exits 0
- shared/schema.ts has sessions table with all required columns
- All 20 FK columns have .references() calls
- package.json has all 3 new scripts and 2 new dependencies
</verification>

<success_criteria>
Schema is hardened with FK constraints and sessions table. Migration tooling is ready. New dependencies installed. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-migration/01-01-SUMMARY.md`
</output>
