---
phase: 01-infrastructure-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/format.ts
  - app/(tabs)/index.tsx
  - app/(tabs)/profile.tsx
  - app/(tabs)/log.tsx
  - app/ruck/[id].tsx
  - app/community/[id].tsx
  - app/notifications.tsx
  - data/mockData.ts
autonomous: true
requirements:
  - D-26
  - D-27

must_haves:
  truths:
    - "formatDuration, formatPace, and timeAgo are defined only in lib/format.ts"
    - "All screen files import these functions from @/lib/format instead of defining locally"
    - "data/mockData.ts is deleted"
  artifacts:
    - path: "lib/format.ts"
      provides: "Canonical formatDuration, formatPace, timeAgo functions"
      exports: ["formatDuration", "formatPace", "timeAgo"]
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "lib/format.ts"
      via: "import statement"
      pattern: "import.*from.*@/lib/format"
---

<objective>
Extract duplicated utility functions (formatDuration, formatPace, timeAgo) into a shared lib/format.ts module and delete the dead mockData.ts file.

Purpose: Eliminates 3x code duplication across screen files (D-26) and removes dead code (D-27). Low-risk, high-value cleanup. Completely independent of other plans.
Output: New lib/format.ts. Updated screen imports. Deleted data/mockData.ts.
</objective>

<execution_context>
@.planning/phases/01-infrastructure-migration/01-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@app/(tabs)/index.tsx
@app/(tabs)/profile.tsx
@app/(tabs)/log.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/format.ts with canonical utility functions</name>
  <files>lib/format.ts</files>
  <read_first>app/(tabs)/index.tsx, app/(tabs)/profile.tsx, app/(tabs)/log.tsx</read_first>
  <action>
Create a new file lib/format.ts with the three utility functions. Use the BEST implementation of each:

- timeAgo: Use the version from app/(tabs)/index.tsx because it handles the "just now" / "< 1 min" edge case. Read lines ~37-65 of index.tsx for the full implementation.
- formatDuration: Read all three implementations and use the most complete one. The function takes seconds (number) and returns a string like "1h 23m" or "45m 12s".
- formatPace: Read all three implementations and use the most complete one. Takes distanceMeters and durationSeconds, returns a pace string.

The file must:
1. Export all three functions as named exports
2. Include TypeScript types for parameters and return values
3. Handle edge cases (zero values, null/undefined inputs where applicable)

```typescript
export function formatDuration(seconds: number): string { ... }
export function formatPace(distanceMeters: number, durationSeconds: number): string { ... }
export function timeAgo(date: Date | string): string { ... }
```
  </action>
  <verify>
    <automated>cd "C:/Users/bpeeb/Ruck On App/RuckON-Mobile" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - lib/format.ts exists
    - lib/format.ts contains `export function formatDuration(`
    - lib/format.ts contains `export function formatPace(`
    - lib/format.ts contains `export function timeAgo(`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>lib/format.ts exists with all three exported functions. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline utilities with imports and delete mockData</name>
  <files>app/(tabs)/index.tsx, app/(tabs)/profile.tsx, app/(tabs)/log.tsx, app/ruck/[id].tsx, app/community/[id].tsx, app/notifications.tsx, data/mockData.ts</files>
  <read_first>app/(tabs)/index.tsx, app/(tabs)/profile.tsx, app/(tabs)/log.tsx, app/ruck/[id].tsx, app/community/[id].tsx, app/notifications.tsx</read_first>
  <action>
For each screen file that defines formatDuration, formatPace, or timeAgo locally:

1. Add import at the top: `import { formatDuration, formatPace, timeAgo } from "@/lib/format";`
   (only import the functions actually used in that file)
2. Delete the local function definitions of formatDuration, formatPace, and timeAgo from the file body
3. Leave all call sites unchanged — they already call these functions by name

Files known to have duplicates (from grep results):
- app/(tabs)/index.tsx — has all three
- app/(tabs)/profile.tsx — has all three
- app/(tabs)/log.tsx — has formatDuration, formatPace
- app/ruck/[id].tsx — check for any of the three
- app/community/[id].tsx — check for any of the three
- app/notifications.tsx — check for timeAgo

Read each file first to confirm which functions are defined locally before editing.

Also: Delete data/mockData.ts entirely (per D-27, it is dead code not imported anywhere).
  </action>
  <verify>
    <automated>cd "C:/Users/bpeeb/Ruck On App/RuckON-Mobile" && npx tsc --noEmit && echo "TSC OK" && node -e "const fs=require('fs'); const files=['app/(tabs)/index.tsx','app/(tabs)/profile.tsx','app/(tabs)/log.tsx']; let found=false; files.forEach(f => { const c=fs.readFileSync(f,'utf8'); if(c.includes('function formatDuration') || c.includes('function timeAgo') || c.includes('function formatPace')) { console.log('FAIL: local definition still in', f); found=true; }}); if(!found) console.log('PASS: no local definitions'); if(fs.existsSync('data/mockData.ts')) { console.log('FAIL: mockData.ts still exists'); } else { console.log('PASS: mockData.ts deleted'); }"</automated>
  </verify>
  <acceptance_criteria>
    - app/(tabs)/index.tsx contains `import { formatDuration, formatPace, timeAgo } from "@/lib/format"` (or subset)
    - app/(tabs)/index.tsx does NOT contain `function formatDuration` or `function timeAgo` or `function formatPace` as local definitions
    - app/(tabs)/profile.tsx contains import from "@/lib/format"
    - app/(tabs)/profile.tsx does NOT contain local definitions of these functions
    - app/(tabs)/log.tsx contains import from "@/lib/format"
    - app/(tabs)/log.tsx does NOT contain local definitions of these functions
    - data/mockData.ts does NOT exist
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All screen files import from lib/format.ts. No local duplicate definitions remain. data/mockData.ts deleted. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit exits 0
- grep for 'function formatDuration' in app/ returns only lib/format.ts
- data/mockData.ts does not exist
</verification>

<success_criteria>
Utility functions consolidated into lib/format.ts. All screens import from the shared module. Dead code removed. No compilation errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-migration/01-02-SUMMARY.md`
</output>
